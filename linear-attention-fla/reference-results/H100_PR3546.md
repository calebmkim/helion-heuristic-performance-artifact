# H100 Reference: Helion PR 3546

> **Temporarily invalidated:** the previous artifact set
> `HELION_AUTOTUNE_EFFORT=none` for the AOT arm. Helion therefore resolved the
> compiler default before consulting `AOTAutotuneCache`, making the reported
> “AOT-tuned” measurements a duplicate seed run. Corrected AOT measurements are
> in progress; do not use the previous table or graph.
